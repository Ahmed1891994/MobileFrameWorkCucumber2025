package Utils.Enums;

// Enum to define WHERE screenshots are stored
public enum ScreenshotStorage {
    LOCAL,       // Save to filesystem
    REPORT,     // Attach to test report
    BOTH        // Do both
}
